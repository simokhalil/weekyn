<html>

<head>
  <title></title>
  <style type="text/css" rel="stylesheet" media="all">
    html, body {
      height: auto;
    }

    * {
      box-sizing: border-box;
    }

    .page {
      position: relative;
      overflow: hidden;
      padding: 30px;
      page-break-after: auto;
    }
    .page.landscape {
        width: 1122px;
        height: 694px;
        background-color: #eee;
    }
    .page.portrait {
        width: 794px;
        height: 1122px;
    }

    .header {
      width: 100%;
      height: 100px;
      border: 1px solid #222;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 20px 0 50px;
    }

    .table-container {
      width: 100%;
      margin-top: 50px;
    }

    .table {
      width: 100%;
      font-size: 12px;
      text-align: center;
      border-spacing: 0;
      border-collapse: collapse;
    }
    .table-first-col {
      width: auto;
      min-height: 20px;
      border: 1px solid #000;
      padding: 5px;
      margin: 0;
      text-align: left;
    }
    .table-col {
      width: 26px;
      min-height: 20px;
      min-width: 26px;
      max-width: 26px;
      border: 1px solid #000;
      padding: 0;
      margin: 0;
    }
  </style>
  <!--[if mso]>
    <style type="text/css">
      .f-fallback  {
        font-family: Arial, sans-serif;
      }
    </style>
  <![endif]-->
</head>

<body class="page landscape">
  <div class="header">
    <div>Rapport d'activité - {{ displayName }} - {{ clientName }} - {{ projectName }} - {{ month}}/{{ year }}</div>
    <div>{{ date }}</div>
  </div>

  <div class="table-container">
    <table class="table">
      <thead>
        <th class="table-first-col">Client / Projet</th>

        {% for day in days %}
          <th class="table-col">{{day}}</th>
        {% endfor %}
      </thead>

      <tr>
        <td class="table-first-col">
          < Client - Projet>
        </td>

        {% for day in days %}
          <td class="table-col">
            {% if activity[day] is defined %}
              {{ activity[day] }}
            {% endif %}
          </td>
        {% endfor %}
      </tr>
    </table>
  </div>
</body>

</html>